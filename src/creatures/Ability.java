package creatures;

// Interface to define a unique ability that each creature can perform
public interface Ability {
    // Method to execute the creature's unique ability
    void executeAbility();
}
